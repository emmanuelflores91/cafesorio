=== EXTRACTO DELTA (solo lo nuevo o modificado) ===
Generado: 2026-08-08T05:14:58.696Z
Este extracto contiene SOLO lo nuevo o modificado respecto del último extracto ya enviado a Claude en este chat. Todo lo que NO está acá sigue vigente tal como lo tiene Claude.
Términos: _renderState @ js/modules/IntroController.js, _mapEvents @ js/modules/IntroController.js, _openComicZoom @ js/modules/IntroController.js, _closeComicZoom @ js/modules/IntroController.js, _openZoom @ js/modules/GalleryController.js, _closeZoom @ js/modules/GalleryController.js, .comic-slide @ css/layout.css, .gallery-grid__zoom @ css/components/gallery.css, .gallery-grid__zoom-img @ css/components/gallery.css, #app-state-intro @ index.html, #app-state-main @ index.html

=== HTML (extractos) ===
### index.html
--- <section> coincide con #app-state-intro @ index.html (líneas 56-76) ---
  <!-- ANCESTRO: <html lang="es"> -->
  <!-- ANCESTRO: <body> -->
<section id="app-state-intro" class="comic-sequence is-hidden">

        <div class="comic-track">
            <img class="comic-slide is-active" src="assets/images/vi-1-web-nv.webp" alt="Viñeta 1 de 5"
                draggable="false">
            <img class="comic-slide" src="assets/images/vi-2-web-nv.webp" alt="Viñeta 2 de 5" draggable="false">
            <img class="comic-slide" src="assets/images/vi-3-web-nv.webp" alt="Viñeta 3 de 5" draggable="false">
            <img class="comic-slide" src="assets/images/vi-4-web-nv.webp" alt="Viñeta 4 de 5" draggable="false">
            <img class="comic-slide" src="assets/images/vi-5-web-nv.webp" alt="Viñeta 5 de 5" draggable="false">
        </div>

        <div class="gallery-grid__zoom comic-zoom is-hidden">
            <img class="gallery-grid__zoom-img" src="" alt="" draggable="false">
            <button class="gallery-grid__zoom-close" type="button" aria-label="Cerrar imagen ampliada">
                <svg class="gallery-grid__zoom-close-icon" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
                    <polyline points="15 18 9 12 15 6" />
                </svg>
            </button>
        </div>

    </section>

--- <div> coincide con #app-state-main @ index.html (líneas 80-163) ---
  <!-- ANCESTRO: <html lang="es"> -->
  <!-- ANCESTRO: <body> -->
<div id="app-state-main" class="is-hidden">

        <!-- Layout Split-Screen: Desktop 60vw/40vw -->
        <main class="layout-split">

            <!-- Panel Izquierdo (60vw): Galería fotográfica inmersiva en Grid -->
            <section class="layout-split__gallery" aria-label="Galería fotográfica del predio">
                <figure class="gallery-grid">

                    <div class="gallery-grid__track" aria-live="polite">
                        <img class="gallery-grid__slide" src="assets/images/chacra-8.jpg" alt="Vista exterior 1" draggable="false">
                        <img class="gallery-grid__slide" src="assets/images/chacra-10.jpg" alt="Vista exterior 2" draggable="false">
                        <img class="gallery-grid__slide" src="assets/images/chacra-4.png" alt="Vista exterior 3" draggable="false">
                        <img class="gallery-grid__slide" src="assets/images/chacra-9.jpg" alt="Vista exterior 4" draggable="false">

                        <div class="gallery-grid__zoom is-hidden">
                            <img class="gallery-grid__zoom-img" src="" alt="" draggable="false">
                            <button class="gallery-grid__zoom-close" type="button" aria-label="Cerrar imagen ampliada">
                                <svg class="gallery-grid__zoom-close-icon" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
                                    <polyline points="15 18 9 12 15 6" />
                                </svg>
                            </button>
                        </div>
                    </div>



                </figure>
            </section>

            <!-- Panel Derecho (40vw): Contenedor de información con degradado -->
            <section class="layout-split__info venue-info" aria-label="Información del evento">

                <div class="venue-info__logo-wrapper">
                    <img src="assets/images/la_milonga.png" class="venue-info__logo" alt="Logo Chacra La Milonga"
                        draggable="false">
                </div>

                <div class="venue-info__body">
                    <p class="venue-info__address">
                        Leandro N. Alem S/N, Esteban Echeverría,<br>
                        Provincia de Buenos Aires
                    </p>
                </div>

                <div class="venue-info__actions">
                    <nav class="venue-info__links" aria-label="Enlaces logísticos">
                        <a href="https://maps.app.goo.gl/bbEqqj2s4zFuCF2G6" class="venue-link" target="_blank"
                            rel="noopener noreferrer">
                            <div class="venue-icon-mask venue-link__icon"
                                style="--icon-url: url('assets/images/ubicacion.png');"></div>
                            <span>Cómo llegar</span>
                        </a>
                        <a href="https://www.instagram.com/chacra.lamilonga/" class="venue-link" target="_blank"
                            rel="noopener noreferrer">
                            <div class="venue-icon-mask venue-link__icon"
                                style="--icon-url: url('assets/images/instagram.png');"></div>
                            <span>Ver en Instagram</span>
                        </a>
                        <a href="https://www.youtube.com/watch?v=p4I1z1CKYNY" class="venue-link" target="_blank"
                            rel="noopener noreferrer">
                            <div class="venue-icon-mask venue-link__icon"
                                style="--icon-url: url('assets/images/camara.png');"></div>
                            <span>Recorrer el predio</span>
                        </a>
                    </nav>

                    <hr class="venue-info__divider">

                    <div class="venue-info__form-section">
                        <a href="https://docs.google.com/forms/d/e/1FAIpQLSfBRKIAf1oDwaejlKdOXCIsbNqcPuWyjJV8ddQSY6-H5wIhkg/viewform"
                            target="_blank" rel="noopener noreferrer" class="venue-form-btn"
                            aria-label="Abrir formulario de asistencia">
                            <div class="venue-icon-mask venue-form-btn__icon"
                                style="--icon-url: url('assets/images/formulario.png');"></div>
                            <span>Formulario de asistencia y<br>consideraciones alimentarias</span>
                        </a>
                    </div>
                </div>

            </section>

        </main>
    </div>

=== CSS (reglas relevantes) ===
### css/components/gallery.css
/* css/components/gallery.css — Galería fotográfica inmersiva (Grid + cinemática) */

/* ─── Matriz de Grilla para Galería ─────────────────────────────────────── */
.gallery-grid {
    width: 100%;
    height: 100%;
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    grid-template-rows: 1fr auto 1fr;
    column-gap: 30px;
    row-gap: 18px;
    align-items: center;
    justify-items: center;
    margin: 0;
    padding: 0;
    overflow: hidden;
    background-color: transparent;
}

/* ─── Track: Bounding Box para proporciones fotográficas ─────────────────── */
.gallery-grid__track {
    grid-column: 2;
    grid-row: 2;
    position: relative;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
    width: 90%;
    max-width: 1200px;
    height: auto;
}

@media (min-width: 768px) {
.gallery-grid__track {
        gap: 32px;
    }
}

/* ─── Cinemática de Extrusión y Desplazamiento ───────────────────────────── */
.gallery-grid__slide {
    width: 100%;
    height: 100%;
    aspect-ratio: 3/2;
    object-fit: cover;
    display: block;
    position: relative;
    opacity: 1;
    box-shadow: 0 8px 35px 3px rgba(0, 0, 0, 0.59);
    cursor: pointer;
}

.gallery-grid__zoom {
    position: absolute;
    inset: 0;
    z-index: 200;
}

.gallery-grid__zoom-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    box-shadow: 0 8px 35px 3px rgba(0, 0, 0, 0.59);
}

.gallery-grid__zoom-close {
    position: absolute;
    top: 12px;
    left: 12px;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: rgba(60, 60, 60, 0.75);
    border-radius: 4px;
    z-index: 210;
    cursor: pointer;
    transition: transform var(--transition-smooth);
}

.gallery-grid__zoom-close:hover,
.gallery-grid__zoom-close:focus-visible {
    transform: scale(1.1);
    outline: none;
}

.gallery-grid__zoom-close-icon {
    width: 22px;
    height: 22px;
    fill: none;
    stroke: #FFFFFF;
    stroke-width: 2;
    stroke-linecap: round;
    stroke-linejoin: round;
    pointer-events: none;
}

@media (orientation: landscape) and (width: 874px) and (height: 402px) {
.gallery-grid__track {
        grid-template-columns: repeat(4, 1fr);
        gap: 8px;
        width: 98%;
    }
}

@media (orientation: landscape) and (width: 874px) and (height: 402px) {
.gallery-grid__slide {
        height: 110px;
        object-fit: contain;
    }
}

### css/components/info-panel.css
/* css/components/info-panel.css — Panel de información del evento (venue-info) */

/* ─── Contenedor principal del panel derecho ─────────────────────────────── */
.venue-info {
    /* background-color: var(--color-panel-right); */
    background: linear-gradient(to right, #c28e58, transparent) height: 100%;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    padding: var(--space-48) var(--space-32) var(--space-48) var(--space-24);
    gap: var(--space-32);
    font-family: var(--font-primary);
    color: #5b4123;
}

@media (max-width: 1023px) {
.venue-info {
        gap: 0;
    }
}

/* ─── Máquina de Estados para Máscaras CSS ───────────────────────────────── */
.venue-icon-mask {
    display: inline-block;
    background-color: #5b4123;
    -webkit-mask-image: var(--icon-url);
    mask-image: var(--icon-url);
    -webkit-mask-size: contain;
    mask-size: contain;
    -webkit-mask-repeat: no-repeat;
    mask-repeat: no-repeat;
    -webkit-mask-position: center;
    mask-position: center;
}

/* ─── Logo principal del venue ───────────────────────────────────────────── */
.venue-info__logo-wrapper {
    margin-bottom: var(--space-16);
    width: 100%;
    max-width: 480px;
    display: flex;
    justify-content: center;
}

.venue-info__logo {
    width: 240px;
    height: auto;
    max-height: 150px;
    object-fit: contain;
}

@media (max-width: 1023px) {
.venue-info__logo-wrapper {
        margin-top: 55px;
    }
}

@media (max-width: 1023px) {
.venue-info__logo {
        width: 156px;
    }
}

/* ─── Contenedor de cuerpo (Alineación estructurada) ─────────────────────── */
.venue-info__body {
    display: flex;
    flex-direction: column;
    gap: var(--space-24);
    width: 100%;
    max-width: 480px;
}

/* ─── Bloque de acciones (links + formulario) — Mobile: 2 columnas ───────── */
.venue-info__actions {
    display: flex;
    flex-direction: row;
    align-items: flex-start;
    justify-content: space-between;
    gap: var(--space-16);
    width: 100%;
    max-width: 480px;
}

@media (max-width: 1023px) {
.venue-info__actions {
        margin-top: 30px;
    }
}

@media (min-width: 1024px) {
.venue-info__actions {
        flex-direction: column;
        align-items: flex-start;
        justify-content: flex-start;
        gap: var(--space-32);
    }
}

/* ─── Dirección postal ───────────────────────────────────────────────────── */
.venue-info__address {
    font-size: var(--text-base);
    text-align: center;
    line-height: 1.6;
    margin: 0;
}

@media (max-width: 1023px) {
.venue-info__address {
        font-size: calc(var(--text-base) * 1.13);
        line-height: 1.35;
    }
}

/* ─── Navegación y Enlaces ───────────────────────────────────────────────── */
.venue-info__links {
    display: flex;
    flex-direction: column;
    gap: var(--space-24);
    width: 100%;
}

.venue-link {
    display: flex;
    align-items: center;
    gap: var(--space-16);
    text-decoration: none;
    color: inherit;
    font-size: var(--text-base);
    transition: opacity var(--transition-smooth);
}

.venue-link:hover,
.venue-link:focus-visible {
    opacity: 0.7;
    outline: none;
}

.venue-link__icon {
    width: 26px;
    height: 26px;
    flex-shrink: 0;
}

/* ─── Separador decorativo ───────────────────────────────────────────────── */
.venue-info__divider {
    width: 1px;
    height: 126px;
    align-self: flex-start;
    border: none;
    border-right: 1px solid var(--color-divider);
    margin: 0;
}

@media (min-width: 1024px) {
.venue-info__divider {
        width: 200px;
        height: auto;
        align-self: auto;
        border-right: none;
        border-bottom: 1px solid var(--color-divider);
    }
}

/* ─── Call to Action (Formulario) ────────────────────────────────────────── */
.venue-info__form-section {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-16);
    text-align: left;
    line-height: 1.4;
}

@media (max-width: 1023px) {
.venue-info__form-section {
        align-items: center;
        text-align: center;
    }
}

.venue-form-btn span {
    font-size: var(--text-lg);
    line-height: 1.4;
    text-align: left;
}

.venue-form-btn {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    gap: var(--space-16);
    padding: 0;
    transition: transform var(--transition-smooth);
}

.venue-form-btn__icon {
    width: 26px;
    height: 26px;
    flex-shrink: 0;
}

@media (max-width: 1023px) {
.venue-form-btn {
        flex-direction: column-reverse;
        align-items: center;
        gap: 16px;
    }
}

@media (max-width: 1023px) {
.venue-form-btn span {
        text-align: center;
        max-width: 110px;
        margin: 0 auto;
        font-size: calc(var(--text-lg) * 0.92);
        line-height: 1.25;
    }
}

@media (max-width: 1023px) {
.venue-form-btn__icon {
        width: 36px;
        height: 36px;
    }
}

.venue-form-btn:hover,
.venue-form-btn:focus-visible {
    transform: scale(1.15);
    outline: none;
}

@media (orientation: landscape) and (width: 874px) and (height: 402px) {
.venue-info {
        display: grid;
        grid-template-columns: 120px 1fr;
        grid-template-rows: auto auto;
        align-items: center;
        justify-content: start;
        padding: var(--space-16);
        gap: 4px var(--space-16);
    }
}

@media (orientation: landscape) and (width: 874px) and (height: 402px) {
.venue-info__body {
        grid-column: 1;
        grid-row: 2;
        flex: 0 0 auto;
        gap: 8px;
        max-width: none;
    }
}

@media (orientation: landscape) and (width: 874px) and (height: 402px) {
.venue-info__logo-wrapper {
        grid-column: 1;
        grid-row: 1;
        margin-top: 0;
        margin-bottom: 0;
    }
}

@media (orientation: landscape) and (width: 874px) and (height: 402px) {
.venue-info__logo {
        width: 100%;
        max-width: 125px;
        max-height: none;
    }
}

@media (orientation: landscape) and (width: 874px) and (height: 402px) {
.venue-info__address {
        font-size: calc(var(--text-base) * 0.9);
        line-height: 1.2;
        text-align: left;
    }
}

@media (orientation: landscape) and (width: 874px) and (height: 402px) {
.venue-info__actions {
        grid-column: 2;
        grid-row: 1 / 3;
        flex-direction: row;
        align-items: center;
        justify-content: flex-start;
        margin-top: 0;
        gap: var(--space-16);
        max-width: none;
    }
}

@media (orientation: landscape) and (width: 874px) and (height: 402px) {
.venue-info__links {
        flex: 0 0 40%;
        flex-direction: column;
        gap: 14px;
    }
}

@media (orientation: landscape) and (width: 874px) and (height: 402px) {
.venue-info__divider {
        width: 1px;
        height: 70px;
        align-self: center;
        border-right: 1px solid var(--color-divider);
        border-bottom: none;
    }
}

@media (orientation: landscape) and (width: 874px) and (height: 402px) {
.venue-info__form-section {
        flex: 1 1 auto;
        align-items: flex-start;
        text-align: left;
    }
}

@media (orientation: landscape) and (width: 874px) and (height: 402px) {
.venue-form-btn {
        max-width: 260px;
        padding: 10px 14px;
        flex-direction: row;
    }
}

### css/layout.css
.layout-split {
    display: flex;
    flex-direction: column;
    width: 100%;
    min-height: 100vh;
    background-color: transparent;
}

/* Galería: tercio superior en Mobile */
.layout-split__gallery {
    width: 100%;
    height: 33.333vh;
    overflow: hidden;
}

/* Panel de información: flujo debajo de la galería en Mobile */
.layout-split__info {
    width: 100%;
    padding: var(--space-32) var(--space-24);
}

@media (min-width: 1024px) {
.layout-split {
        flex-direction: row;
        height: 100vh;
        overflow: hidden;
    }
}

@media (min-width: 1024px) {
.layout-split__gallery {
        width: 60vw;
        height: 100vh;
    }
}

@media (min-width: 1024px) {
.layout-split__info {
        width: 40vw;
        height: 100vh;
        padding: 0;
        overflow: hidden;
    }
}

/* ─── Utilidad de ocultación ─────────────────────────────────────────────── */
.is-hidden {
    display: none !important;
}

/* ─── Estado inicial: secuencia de viñetas cómicas ───────────────────────────────────────
 * Ocupa el viewport completo y centra el track de slides.
 * ────────────────────────────────────────────────────────────────────────────── */
.comic-sequence {
    width: 100vw;
    height: 100vh;
    display: grid;
    /* Mutación: Grid 3×3 — columna/fila central envuelve el track */
    grid-template-columns: 1fr;
    grid-template-rows: 1fr auto;
    /* Mutación: Reducción exclusiva del gap vertical a 18px para acercar los indicadores */
    row-gap: 18px;
    align-items: center;
    justify-items: center;
    position: relative;
    z-index: 50;
    overflow: hidden;
}

/* ─── Track: contexto de apilamiento para las viñetas ─────────────────────────── */
.comic-track {
    grid-column: 1;
    grid-row: 1;
    position: relative;
    margin-top: 8px;
    /* Mutación: Recálculo del Bounding Box para dejar espacio a los controles inferiores fijos */
    width: min(calc(100vw - 32px), calc((100vh - 160px) * (1920 / 1285)));
    aspect-ratio: 1920 / 1285;
    cursor: pointer;
}

/* ─── Slide individual de la secuencia intro ─────────────────────────────────────────── */
.comic-slide {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    /* Mutación: Extrusión volumétrica al contenedor matemáticamente exacto */
    object-fit: fill;
    opacity: 1;
    /* Mutación: Centrado delegado al Grid; traslación de eje X simplificada */
    transform: translateX(150vw);
    transition: transform 0.6s cubic-bezier(0.25, 0.8, 0.25, 1);
    z-index: 0;
    border: none;
    /* Mutación: +30% canal alfa (0.45→0.59), +10% expansión (35px blur, 3px spread) */
    box-shadow: 0 8px 35px 3px rgba(0, 0, 0, 0.59);
}

/* ─── Estado activo del slide (Viewport central) ──────────────────────────────── */
.comic-slide.is-active {
    transform: translateX(0);
    z-index: 1;
}

/* ─── Estado pasado del slide (Búfer izquierdo off-screen) ──────────────────────── */
.comic-slide.is-past {
    transform: translateX(-150vw);
    z-index: 0;
}

.comic-dot.is-active {
    opacity: 1;
    transform: scale(1.3);
}

=== JS (bloques relevantes) ===
### js/modules/GalleryController.js
// --- líneas 3-46 ---
class GalleryController {
    /**
     * @param {HTMLElement} rootElement - Elemento raíz de la galería (.gallery-grid)
     */
    constructor(rootElement) {
        this._nodeRoot      = rootElement;
        this._nodeTrack     = rootElement.querySelector('.gallery-grid__track');
        this._nodeZoom      = rootElement.querySelector('.gallery-grid__zoom');
        this._nodeZoomImg   = rootElement.querySelector('.gallery-grid__zoom-img');
        this._nodeZoomClose = rootElement.querySelector('.gallery-grid__zoom-close');

        if (!this._nodeTrack || !this._nodeZoom || !this._nodeZoomImg || !this._nodeZoomClose) {
            console.warn('GalleryController: fallo en la resolución de nodos requeridos.');
            return;
        }

        this._slides = Array.from(this._nodeTrack.querySelectorAll('.gallery-grid__slide'));
        this._mapEvents();
    }

    // ── Mapeo de eventos ───────────────────────────────────────────────────
    _mapEvents() {
        this._slides.forEach((slide) => {
            slide.addEventListener('click', () => this._openZoom(slide));
        });

        this._nodeZoomClose.addEventListener('click', (e) => {
            e.stopPropagation();
            this._closeZoom();
        });
    }

    // ── Abrir imagen ampliada ──────────────────────────────────────────────
    _openZoom(slide) {
        this._nodeZoomImg.src = slide.src;
        this._nodeZoomImg.alt = slide.alt;
        this._nodeZoom.classList.remove('is-hidden');
    }

    // ── Cerrar imagen ampliada ─────────────────────────────────────────────
    _closeZoom() {
        this._nodeZoom.classList.add('is-hidden');
    }
}

=== REPORTE ===
✔ _renderState @ js/modules/IntroController.js → js/modules/IntroController.js (js: 7)
✔ _mapEvents @ js/modules/IntroController.js → js/modules/IntroController.js (js: 2)
✔ _openComicZoom @ js/modules/IntroController.js → js/modules/IntroController.js (js: 2)
✔ _closeComicZoom @ js/modules/IntroController.js → js/modules/IntroController.js (js: 2)
✔ _openZoom @ js/modules/GalleryController.js → js/modules/GalleryController.js (js: 2)
✔ _closeZoom @ js/modules/GalleryController.js → js/modules/GalleryController.js (js: 2)
✔ .comic-slide @ css/layout.css → css/layout.css (css: 3)
✔ .gallery-grid__zoom @ css/components/gallery.css → css/components/gallery.css (css: 1)
✔ .gallery-grid__zoom-img @ css/components/gallery.css → css/components/gallery.css (css: 1)
✔ #app-state-intro @ index.html → index.html (html: 1)
✔ #app-state-main @ index.html → index.html (html: 1)

=== YA ENVIADO ANTES (sin cambios, no se repite) ===
- css/layout.css (css: 3 bloque/s sin cambios)
- js/main.js (js: 2 bloque/s sin cambios)
- js/modules/IntroController.js (js: 1 bloque/s sin cambios)