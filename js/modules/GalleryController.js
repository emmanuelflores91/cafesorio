// js/modules/GalleryController.js — Controlador estático de la galería fotográfica

class GalleryController {
    /**
     * @param {HTMLElement} rootElement - Elemento raíz de la galería (.gallery-grid)
     */
    constructor(rootElement) {
        // Constructor vacío: El renderizado y diseño responsivo es estrictamente estático
        // y delegado al motor CSS (grid-template-columns), sin paginación JS.
    }
}

export default GalleryController;